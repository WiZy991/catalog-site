# Применение миграций на сервере

## ⚠️ Проблема
Ошибка: `no such table: catalog_synclog`

Это означает, что миграции не применены к базе данных на сервере.

## 🔧 Решение

### Шаг 1: Загрузите миграцию на сервер

Загрузите файл `catalog/migrations/0012_synclog.py` на сервер в папку:
```
/home/o/onesim8n/onesimus/onesimus/catalog/migrations/
```

### Шаг 2: Примените миграции

**Вариант 1: Через SSH (если есть доступ)**

Подключитесь к серверу по SSH и выполните:
```bash
cd /home/o/onesim8n/onesimus/onesimus
source venv/bin/activate
python manage.py migrate catalog
```

**Вариант 2: Через панель Beget**

1. Зайдите в панель управления Beget
2. Найдите раздел "Управление проектом" или "Django"
3. Найдите кнопку "Применить миграции" или "Migrate"
4. Или используйте "Консоль" / "Terminal" для выполнения команд

**Вариант 3: Через файловый менеджер Beget**

1. Зайдите в файловый менеджер
2. Найдите файл `manage.py` в корне проекта
3. Через консоль Beget выполните:
   ```bash
   cd /home/o/onesim8n/onesimus/onesimus
   source venv/bin/activate
   python manage.py migrate catalog
   ```

### Шаг 3: Проверьте результат

После применения миграций:
1. Зайдите в админку: `/admin/catalog/synclog/`
2. Ошибка должна исчезнуть
3. Должна появиться страница с логами синхронизации (пока пустая)

## 📋 Чек-лист:

- [ ] Файл `catalog/migrations/0012_synclog.py` загружен на сервер
- [ ] Миграции применены командой `python manage.py migrate catalog`
- [ ] Страница `/admin/catalog/synclog/` открывается без ошибок
- [ ] Таблица `catalog_synclog` создана в базе данных

## 💡 Важно:

- Миграции нужно применять на сервере, где работает Django
- После применения миграций таблица `catalog_synclog` будет создана
- После этого можно будет использовать логи синхронизации

## 🚨 Если миграции не применяются:

1. Проверьте, что файл миграции загружен правильно
2. Проверьте права доступа к базе данных
3. Проверьте, что Django имеет доступ к базе данных
4. Проверьте логи Django на наличие ошибок
