# Каталог товаров с интеграцией 1С и Farpost

Веб-сайт каталога товаров с возможностью автоматического и ручного обмена данными между 1С, сайтом и площадкой Farpost.

## Возможности

- 📦 Каталог товаров с категориями и подкатегориями (неограниченная вложенность)
- 🔍 Фильтрация товаров (цена, бренд, состояние, наличие и др.)
- 🖼️ Галерея изображений для каждого товара
- 🔗 Интеграция с Farpost (кнопка перехода на объявление)
- 📥 Импорт товаров из CSV/XLS файлов
- 📤 Экспорт прайса для Farpost
- 🔧 Удобная административная панель
- 📱 Адаптивный дизайн
- 🔎 SEO-оптимизация (ЧПУ, sitemap, микроразметка Schema.org)

## Установка

### 1. Создайте виртуальное окружение

```bash
python -m venv venv
```

### 2. Активируйте виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Примените миграции

```bash
python manage.py migrate
```

### 5. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

### 6. Запустите сервер разработки

```bash
python manage.py runserver
```

Сайт будет доступен по адресу: http://127.0.0.1:8000/

Административная панель: http://127.0.0.1:8000/admin/

## Структура проекта

```
├── config/             # Настройки Django
├── catalog/            # Приложение каталога
│   ├── models.py       # Модели (Category, Product, ProductImage)
│   ├── admin.py        # Настройка админ-панели
│   ├── views.py        # Представления
│   └── filters.py      # Фильтры каталога
├── core/               # Общие компоненты
├── templates/          # HTML шаблоны
├── static/             # Статические файлы (CSS, JS)
└── media/              # Загруженные изображения
```

## Импорт товаров

1. Перейдите в административную панель
2. Выберите раздел "Товары"
3. Нажмите кнопку "Импорт"
4. Загрузите файл CSV или XLS с товарами

### Формат файла импорта

| Поле | Описание |
|------|----------|
| name | Название товара |
| article | Артикул |
| brand | Бренд |
| price | Цена |
| category | Категория (slug) |
| description | Описание |
| condition | Состояние (new/used) |
| availability | Наличие (in_stock/order/out_of_stock) |
| cross_numbers | Кросс-номера |
| applicability | Применимость |
| farpost_url | Ссылка на Farpost |

## Экспорт для Farpost

1. Перейдите в административную панель
2. Выберите раздел "Товары"
3. Выберите нужные товары
4. Выберите действие "Экспорт для Farpost"
5. Скачайте сгенерированный файл

## API для 1С

Для автоматического обмена с 1С используйте:

- `POST /api/import/` - импорт товаров (JSON/XML)
- `GET /api/export/farpost/` - получение прайса для Farpost

## Лицензия

MIT License

