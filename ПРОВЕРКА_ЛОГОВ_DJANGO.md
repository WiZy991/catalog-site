# Проверка логов Django для диагностики

## ✅ Хорошая новость
Endpoint работает - Django обрабатывает запросы!

## ⚠️ Проблема
Файл `commerceml_requests.log` не создается, хотя endpoint работает.

## 🔍 Что проверить СЕЙЧАС:

### 1. Проверьте логи Django

**Файл:** `logs/django.log`

**Что искать:**
Откройте файл `logs/django.log` и ищите строки, начинающиеся с:
- `CommerceML LOG:`

**Должны быть записи:**
- `CommerceML LOG: BASE_DIR=...`
- `CommerceML LOG: logs_dir=...`
- `CommerceML LOG: log_file_path=...`
- `CommerceML LOG: logs_dir exists=...`
- `CommerceML LOG: logs_dir is writable=...`
- `CommerceML LOG: Директория logs/ создана или уже существует`
- `CommerceML LOG: Проверка записи в logs/ прошла успешно` (или ошибка)
- `CommerceML LOG: Файл успешно записан: ...` (или ошибка)

### 2. Проверьте ошибки

**Ищите в логах:**
- `Ошибка создания директории logs/`
- `Ошибка записи в файл логов`
- `НЕ МОЖЕМ записать в logs/`

### 3. Проверьте пути

**В логах должны быть:**
- `BASE_DIR=...` - путь к проекту
- `logs_dir=...` - путь к директории logs/
- `log_file_path=...` - полный путь к файлу логов

**Проверьте:**
- Правильный ли путь к `BASE_DIR`?
- Существует ли директория `logs/` по этому пути?
- Есть ли права на запись в эту директорию?

## 🔧 Что делать:

### Шаг 1: Откройте endpoint еще раз

1. Откройте в браузере: `http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth`
2. Должно появиться: `failure Ошибка авторизации`

### Шаг 2: Сразу проверьте логи Django

1. Откройте файл `logs/django.log`
2. Прокрутите вниз до последних записей
3. Ищите строки с `CommerceML LOG:`

### Шаг 3: Анализируйте результаты

**Если видите:**
- `logs_dir exists=False` → директория не существует, нужно создать
- `logs_dir is writable=False` → нет прав на запись, нужно исправить права
- `НЕ МОЖЕМ записать в logs/` → проблема с правами доступа
- `Ошибка записи в файл логов` → проблема с записью в файл

**Если видите:**
- `Файл успешно записан` → файл должен быть создан, проверьте путь
- `Файл существует: True` → файл создан, но возможно в другом месте

## 📋 Чек-лист:

- [ ] Endpoint открывается в браузере
- [ ] В `logs/django.log` есть записи с `CommerceML LOG:`
- [ ] В логах указан правильный `BASE_DIR`
- [ ] В логах указан правильный путь к `logs_dir`
- [ ] В логах `logs_dir exists=True`
- [ ] В логах `logs_dir is writable=True`
- [ ] В логах нет ошибок записи
- [ ] Файл `commerceml_requests.log` создается

## 💡 Важно:

- Все диагностические сообщения теперь пишутся в `logs/django.log`
- Проверьте последние записи в логе после открытия endpoint
- Если видите ошибки - они точно укажут на проблему

## 🚨 Если файл все еще не создается:

1. Проверьте логи Django - там будет точная причина
2. Проверьте права доступа к директории проекта
3. Проверьте, что веб-сервер имеет права на запись
4. Проверьте, что `BASE_DIR` указывает на правильную директорию
