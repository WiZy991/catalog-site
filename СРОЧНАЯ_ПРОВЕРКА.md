# СРОЧНАЯ ПРОВЕРКА - Запросы не доходят до Django

## ⚠️ Проблема
Запросы от 1С не доходят до Django - нет логов и файлов не сохраняются.

## 🔍 Что проверить ПРЯМО СЕЙЧАС:

### 1. Проверьте тестовый endpoint в браузере:
Откройте в браузере:
```
http://onesim8n.beget.tech/cml/test/
```

**Ожидаемый результат:** Должно появиться `OK - CommerceML endpoint доступен`

**Если не открывается:**
- Проблема с доступностью сайта
- Проблема с веб-сервером
- Проверьте, что Django запущен

### 2. Проверьте основной endpoint в браузере:
Откройте в браузере:
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Ожидаемый результат:** Должно появиться `failure\nОшибка авторизации` (это нормально!)

**Если не открывается:**
- Проблема с URL или маршрутизацией
- Проверьте настройки на Beget

### 3. Проверьте URL в настройках 1С:

**Правильный URL:**
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Проверьте:**
- Нет ли лишних слэшей
- Правильный ли домен
- Есть ли `http://` в начале

### 4. Проверьте файл логов на сервере:

**Файл:** `logs/commerceml_requests.log`

**Как проверить:**
1. Зайдите на сервер через FTP/SSH
2. Откройте файл `logs/commerceml_requests.log`
3. Если файл пустой или нет записей → запросы НЕ доходят до Django

**Если файла нет:**
- Создайте директорию `logs/` если её нет
- Проверьте права доступа

### 5. Выполните обмен в 1С и сразу проверьте:

1. **В 1С:** Нажмите "Выполнить обмен данными"
2. **Сразу после этого:**
   - Откройте `logs/commerceml_requests.log` - должны появиться новые записи
   - Откройте `logs/django.log` - должны появиться записи
   - Проверьте `media/1c_exchange/` - должны появиться файлы

## 🚨 Если запросы НЕ доходят:

### Возможные причины:

1. **Неправильный URL в 1С**
   - Проверьте URL еще раз
   - Попробуйте с `https://` вместо `http://`
   - Попробуйте без параметров: `http://onesim8n.beget.tech/cml/exchange/`

2. **Проблемы с сетью/доступностью**
   - Может быть блокировка на уровне Beget
   - Может быть проблема с прокси
   - Проверьте, доступен ли сайт из сети, где находится 1С

3. **Проблемы с веб-сервером**
   - Проверьте логи nginx/apache
   - Проверьте, что Django запущен
   - Проверьте настройки на Beget

4. **Проблемы с авторизацией**
   - Если запросы доходят, но не проходят авторизацию
   - Проверьте логин и пароль в 1С
   - Проверьте, что пользователь Django активен и имеет права staff

## 📋 Чек-лист:

- [ ] Тестовый endpoint `/cml/test/` открывается в браузере
- [ ] Основной endpoint открывается (даже с ошибкой авторизации)
- [ ] URL в 1С правильный
- [ ] Файл `logs/commerceml_requests.log` создается
- [ ] После обмена в 1С появляются записи в логах
- [ ] Директория `media/1c_exchange/` существует
- [ ] Права доступа к директории правильные

## 🔧 Что было добавлено для диагностики:

1. **Тестовый endpoint** `/cml/test/` - для проверки доступности
2. **Файл логов** `logs/commerceml_requests.log` - все запросы логируются туда
3. **Улучшенное логирование** - все запросы логируются в файл и в Django лог

## 📞 Что прислать, если не работает:

1. Скриншот настроек узла обмена в 1С (с URL)
2. Результат открытия `/cml/test/` в браузере
3. Содержимое `logs/commerceml_requests.log` (если файл есть)
4. Последние 50 строк из `logs/django.log`
5. Результат проверки подключения в 1С
