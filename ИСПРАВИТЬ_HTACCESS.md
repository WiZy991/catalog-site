# Исправление ошибки в .htaccess

## ❌ Проблема
После добавления `PassengerRequestTimeout 3600` сайт выдает Internal Server Error.

## ✅ Решение

### Вариант 1: Удалить строку (если не поддерживается)

Если `PassengerRequestTimeout` не поддерживается в `.htaccess`, удалите эту строку и верните файл к исходному виду:

```
PassengerEnabled On
PassengerPython /home/o/onesim8n/onesimus/venv/bin/python
```

### Вариант 2: Использовать правильный синтаксис

Попробуйте добавить через `PassengerAppRoot` или использовать другой формат:

```
PassengerEnabled On
PassengerPython /home/o/onesim8n/onesimus/venv/bin/python
PassengerAppRoot /home/o/onesim8n/onesimus/onesimus
```

### Вариант 3: Настройка через passenger_wsgi.py

Вместо `.htaccess`, добавьте настройку в `passenger_wsgi.py`:

```python
import os
os.environ['PASSENGER_REQUEST_TIMEOUT'] = '3600'
```

## 🔧 Быстрое исправление

**Верните файл `.htaccess` к исходному виду:**

```
PassengerEnabled On
PassengerPython /home/o/onesim8n/onesimus/venv/bin/python
```

**Удалите строку `PassengerRequestTimeout 3600`**

## 📋 Альтернативное решение

Так как настройка через `.htaccess` не работает, нужно:

1. **Обратиться в поддержку Beget** для увеличения таймаутов nginx
2. **Или оптимизировать обработку** - разбить XML на части

## ⚠️ Важно

После исправления `.htaccess`:
1. Сохраните файл
2. Перезапустите приложение: `touch tmp/restart.txt`
3. Проверьте, что сайт снова работает
