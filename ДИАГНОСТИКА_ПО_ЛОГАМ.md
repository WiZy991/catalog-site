# Диагностика по логам

## 📊 Текущая ситуация

В `django.log` видны только тестовые записи:
- `INFO 2026-02-11 15:47:57,558 <console> ТЕСТ: Сообщение из shell`
- `ERROR 2026-02-11 15:47:57,559 <console> TEСТ: Ошибка из shell`

**Это означает:**
- ✅ Django работает и пишет в логи
- ❌ Запросов от 1С в логах НЕТ
- ❌ Запросы от 1С не доходят до Django

## 🔍 Что проверить:

### 1. Проверьте файл `logs/commerceml_requests.log`

**Путь:** `logs/commerceml_requests.log`

**Что искать:**
- Если файл существует и содержит записи → запросы доходят
- Если файл пустой или нет записей → запросы НЕ доходят
- Если файла нет → возможно, запросы не доходят или код не обновлен на сервере

**Как проверить:**
1. Зайдите в файловый менеджер Beget
2. Перейдите в `logs/`
3. Откройте файл `commerceml_requests.log` (если есть)
4. Проверьте, есть ли записи с датой/временем обмена

### 2. Проверьте основной endpoint в браузере

Откройте в браузере:
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Ожидаемый результат:**
- Должно появиться: `failure\nОшибка авторизации`
- Это нормально - значит endpoint работает

**Если не открывается:**
- Проблема с доступностью сайта
- Проблема с маршрутизацией

### 3. Проверьте URL в настройках 1С

**Правильный URL:**
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Проверьте:**
- Нет ли лишних слэшей
- Правильный ли домен
- Есть ли `http://` в начале
- Нет ли пробелов

### 4. Выполните обмен в 1С и сразу проверьте логи

**Шаги:**
1. В 1С нажмите "Выполнить обмен данными"
2. **Сразу после этого** (в течение 10 секунд):
   - Откройте `logs/commerceml_requests.log` - должны появиться новые записи
   - Откройте `logs/django.log` - должны появиться записи с "CommerceML запрос получен!"
   - Проверьте `media/1c_exchange/` - должны появиться файлы

## 🚨 Если запросы НЕ доходят:

### Возможные причины:

1. **Неправильный URL в 1С**
   - Проверьте URL еще раз
   - Попробуйте с `https://` вместо `http://`
   - Попробуйте без параметров: `http://onesim8n.beget.tech/cml/exchange/`

2. **Проблемы с доступностью**
   - Может быть блокировка на уровне Beget
   - Может быть проблема с прокси/балансировщиком
   - Проверьте, доступен ли сайт из сети, где находится 1С

3. **Код не обновлен на сервере**
   - Убедитесь, что файл `catalog/commerceml_views.py` обновлен на сервере
   - Убедитесь, что Django перезапущен после обновления кода

4. **Проблемы с веб-сервером**
   - Проверьте логи nginx/apache
   - Проверьте, что Django запущен
   - Проверьте настройки на Beget

## 📋 Чек-лист:

- [ ] Файл `logs/commerceml_requests.log` существует
- [ ] В `logs/commerceml_requests.log` есть записи после обмена
- [ ] Endpoint `/cml/exchange?type=catalog&mode=checkauth` открывается в браузере
- [ ] URL в 1С правильный
- [ ] В 1С проверка подключения проходит успешно
- [ ] Код обновлен на сервере
- [ ] Django перезапущен после обновления кода

## 🔧 Что делать:

1. **Проверьте файл `logs/commerceml_requests.log`**
   - Если файла нет → создайте директорию `logs/` если её нет
   - Если файл пустой → запросы не доходят

2. **Проверьте endpoint в браузере**
   - Откройте URL выше
   - Если не открывается → проблема с доступностью

3. **Проверьте URL в 1С**
   - Убедитесь, что URL правильный
   - Попробуйте выполнить "Проверка подключения" в 1С

4. **Выполните обмен и сразу проверьте логи**
   - Нажмите "Выполнить обмен данными" в 1С
   - Сразу откройте `logs/commerceml_requests.log`
   - Если записей нет → запросы не доходят

## 💡 Важно:

- Если в `django.log` нет записей о CommerceML → запросы не доходят до Django
- Если endpoint открывается в браузере → Django работает правильно
- Если в 1С "Проверка подключения" проходит → URL правильный
- Если после обмена нет записей в логах → запросы не доходят (проблема с URL, сетью или настройками)

## 📞 Что прислать для диагностики:

1. Содержимое файла `logs/commerceml_requests.log` (если файл есть)
2. Результат открытия `/cml/exchange?type=catalog&mode=checkauth` в браузере
3. Скриншот настроек узла обмена в 1С (с URL)
4. Результат проверки подключения в 1С
5. Последние 100 строк из `logs/django.log` (после выполнения обмена)
