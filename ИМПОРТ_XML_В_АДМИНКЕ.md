# Импорт XML файлов в админке

## ✅ Что сделано:

1. **Добавлена поддержка XML** в форму массового импорта
2. **Поддержка CommerceML 2 формата** - можно импортировать XML от 1С
3. **Импорт по external_id** - товары обновляются по ID из 1С
4. **Использование той же логики** что и для Excel/CSV

## 🔧 Как использовать:

### Шаг 1: Зайдите в админку

1. Откройте: `/admin/catalog/bulk-import/`
2. Или через меню: **Каталог → Массовый импорт**

### Шаг 2: Выберите XML файл

1. Нажмите **"Выберите файл"**
2. Выберите XML файл (формат CommerceML 2)
3. Формат теперь поддерживается: **CSV, XLS, XLSX, XML**

### Шаг 3: Настройте параметры

- ✅ **Автоматически определять категории** - рекомендуется включить
- ✅ **Автоматически определять бренды** - рекомендуется включить
- ✅ **Обновлять существующие товары** - рекомендуется включить

### Шаг 4: Импортируйте

1. Нажмите **"Импортировать"**
2. Дождитесь завершения
3. Проверьте результат

## 📋 Что импортируется из XML:

- ✅ **Ид (external_id)** - используется для поиска/обновления товаров
- ✅ **Артикул** - из поля Артикул
- ✅ **Название** - из поля Наименование
- ✅ **Бренд** - определяется автоматически из названия
- ✅ **Категория** - из группы товаров или определяется автоматически
- ✅ **Цена** - из поля ЦенаЗаЕдиницу
- ✅ **Остаток** - из поля Количество
- ✅ **Описание** - из поля Описание
- ✅ **Характеристики** - из ЗначенияСвойств

## 💡 Как работает обновление:

1. **По external_id** (приоритет) - если товар с таким ID есть, он обновляется
2. **По артикулу** - если external_id нет, ищется по артикулу
3. **По названию** - если артикула нет, ищется по названию
4. **Создание нового** - если товар не найден, создается новый

## 🔍 Формат XML файла:

Файл должен быть в формате **CommerceML 2**:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<КоммерческаяИнформация>
    <Каталог>
        <Товары>
            <Товар>
                <Ид>12345</Ид>
                <Артикул>ABC123</Артикул>
                <Наименование>Название товара</Наименование>
                <Описание>Описание товара</Описание>
                <Группы>
                    <Ид>Категория</Ид>
                </Группы>
            </Товар>
        </Товары>
    </Каталог>
</КоммерческаяИнформация>
```

## ⚠️ Важно:

- XML файл должен быть в формате **CommerceML 2**
- Должны быть поля **Ид** и **Наименование** (обязательные)
- Товары обновляются по **external_id** (Ид из XML)
- Все товары создаются в **основном каталоге** (`catalog_type='retail'`)

## 🚨 Если импорт не работает:

1. Проверьте формат XML - должен быть CommerceML 2
2. Проверьте наличие полей Ид и Наименование
3. Проверьте логи Django на ошибки
4. Убедитесь, что файл не поврежден

## 📊 После импорта:

- Проверьте товары в админке: `/admin/catalog/product/`
- Отфильтруйте по `external_id` (не пустое)
- Должны быть товары с `external_id` из XML
