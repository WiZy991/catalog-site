# Пошаговая инструкция по настройке интеграции 1С с Django сайтом

## Часть 1: Настройка на стороне сайта (Django)

### Шаг 1: Выполните миграции базы данных

Откройте терминал в корне проекта и выполните:

```bash
python manage.py makemigrations catalog
python manage.py migrate
```

Это создаст таблицы `ProductCharacteristic` и `SyncLog` в базе данных.

### Шаг 2: Настройте токен авторизации

Откройте файл `config/settings.py` и найдите строку:

```python
ONE_C_API_KEY = os.environ.get('ONE_C_API_KEY', 'change-this-secret-key-in-production')
```

Замените значение на ваш секретный токен:

```python
ONE_C_API_KEY = 'мой-секретный-токен-для-1с-2024'
```

**Важно:** 
- Используйте сложный токен (минимум 20 символов)
- Не используйте простые слова или даты
- Храните токен в безопасности

### Шаг 3: Проверьте настройки email (опционально)

Если хотите получать уведомления об ошибках, убедитесь, что в `settings.py` настроен:

```python
MANAGER_EMAIL = 'ваш-email@example.com'
```

### Шаг 4: Запустите сервер разработки

```bash
python manage.py runserver
```

Сервер будет доступен по адресу: `http://localhost:8000`

**Для продакшена:** Используйте ваш реальный домен, например: `https://your-site.com`

---

## Часть 2: Настройка на стороне 1С

### Шаг 1: Создайте внешнюю обработку или модуль в 1С

Создайте новую внешнюю обработку в 1С (например, `ОбменССайтом.epf`) или добавьте код в существующий модуль.

### Шаг 2: Код для отправки данных из 1С

#### Вариант А: Отправка через API (JSON)

```1c
// Функция для отправки товаров на сайт через API
Функция ОтправитьТоварыНаСайт(МассивТоваров, URLСайта, ТокенАвторизации) Экспорт
    
    // Формируем JSON данные
    СтруктураДанных = Новый Структура;
    СтруктураДанных.Вставить("token", ТокенАвторизации);
    
    МассивТоваровJSON = Новый Массив;
    
    Для Каждого Товар Из МассивТоваров Цикл
        
        ТоварJSON = Новый Структура;
        ТоварJSON.Вставить("sku", Товар.Артикул);
        ТоварJSON.Вставить("name", Товар.Наименование);
        ТоварJSON.Вставить("description", Товар.Описание);
        ТоварJSON.Вставить("price", Товар.Цена);
        ТоварJSON.Вставить("old_price", Товар.СтараяЦена);
        ТоварJSON.Вставить("stock", Товар.Остаток);
        ТоварJSON.Вставить("category", Товар.Категория);
        ТоварJSON.Вставить("is_active", Товар.Активен);
        
        // Характеристики товара
        МассивХарактеристик = Новый Массив;
        Для Каждого Характеристика Из Товар.Характеристики Цикл
            ХарактеристикаJSON = Новый Структура;
            ХарактеристикаJSON.Вставить("name", Характеристика.Название);
            ХарактеристикаJSON.Вставить("value", Характеристика.Значение);
            МассивХарактеристик.Добавить(ХарактеристикаJSON);
        КонецЦикла;
        
        ТоварJSON.Вставить("characteristics", МассивХарактеристик);
        МассивТоваровJSON.Добавить(ТоварJSON);
        
    КонецЦикла;
    
    СтруктураДанных.Вставить("products", МассивТоваровJSON);
    
    // Преобразуем в JSON строку
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, СтруктураДанных);
    СтрокаJSON = ЗаписьJSON.Закрыть();
    
    // Настраиваем HTTP соединение
    Соединение = Новый HTTPСоединение(
        "your-site.com",  // Замените на ваш домен
        443,              // Порт (443 для HTTPS, 80 для HTTP)
        ,                 // Прокси
        ,                 // Пользователь прокси
        ,                 // Пароль прокси
        30,               // Таймаут
        Новый ЗащищенноеСоединениеOpenSSL()  // Для HTTPS
    );
    
    // Формируем заголовки
    Заголовки = Новый Соответствие;
    Заголовки["Content-Type"] = "application/json; charset=utf-8";
    Заголовки["X-API-Key"] = ТокенАвторизации;
    
    // Отправляем запрос
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки("POST", "/api/1c/sync/", Заголовки, СтрокаJSON);
        
        Если Ответ.КодСостояния = 200 Или Ответ.КодСостояния = 207 Тогда
            // Успешно
            Сообщить("Товары успешно отправлены на сайт!");
            
            // Читаем ответ
            ЧтениеJSON = Новый ЧтениеJSON;
            ЧтениеJSON.УстановитьСтроку(Ответ.ПолучитьТелоКакСтроку());
            Результат = ПрочитатьJSON(ЧтениеJSON);
            ЧтениеJSON.Закрыть();
            
            Сообщить("Обработано: " + Результат.processed);
            Сообщить("Создано: " + Результат.created);
            Сообщить("Обновлено: " + Результат.updated);
            
            Если Результат.Свойство("errors") И Результат.errors.Количество() > 0 Тогда
                Сообщить("Ошибки: " + Результат.errors.Количество());
            КонецЕсли;
            
        Иначе
            Сообщить("Ошибка отправки! Код: " + Ответ.КодСостояния);
            Сообщить("Ответ: " + Ответ.ПолучитьТелоКакСтроку());
        КонецЕсли;
        
    Исключение
        Сообщить("Ошибка при отправке данных: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецФункции
```

#### Вариант Б: Отправка файла (CSV/XML/JSON)

```1c
// Функция для отправки файла на сайт
Функция ОтправитьФайлНаСайт(ПутьКФайлу, URLСайта, ТокенАвторизации) Экспорт
    
    // Читаем файл
    Файл = Новый Файл(ПутьКФайлу);
    Если НЕ Файл.Существует() Тогда
        ВызватьИсключение "Файл не найден: " + ПутьКФайлу;
    КонецЕсли;
    
    ДвоичныеДанные = ПолучитьИзВременногоХранилища(ПутьКФайлу);
    
    // Настраиваем HTTP соединение
    Соединение = Новый HTTPСоединение(
        "your-site.com",
        443,
        , , , 30,
        Новый ЗащищенноеСоединениеOpenSSL()
    );
    
    // Формируем заголовки
    Заголовки = Новый Соответствие;
    Заголовки["X-API-Key"] = ТокенАвторизации;
    
    // Отправляем файл
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки("POST", "/api/1c/upload/", Заголовки, ДвоичныеДанные);
        
        Если Ответ.КодСостояния = 200 Или Ответ.КодСостояния = 207 Тогда
            Сообщить("Файл успешно загружен на сайт!");
            
            // Читаем ответ
            ЧтениеJSON = Новый ЧтениеJSON;
            ЧтениеJSON.УстановитьСтроку(Ответ.ПолучитьТелоКакСтроку());
            Результат = ПрочитатьJSON(ЧтениеJSON);
            ЧтениеJSON.Закрыть();
            
            Сообщить("Обработано: " + Результат.processed);
            Сообщить("Создано: " + Результат.created);
            Сообщить("Обновлено: " + Результат.updated);
            
        Иначе
            Сообщить("Ошибка загрузки! Код: " + Ответ.КодСостояния);
            Сообщить("Ответ: " + Ответ.ПолучитьТелоКакСтроку());
        КонецЕсли;
        
    Исключение
        Сообщить("Ошибка при отправке файла: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецФункции
```

### Шаг 3: Пример использования в 1С

```1c
// Пример: Отправка товаров из справочника "Номенклатура"
Процедура ОтправитьТоварыНаСайт()
    
    // Параметры подключения
    URLСайта = "https://your-site.com";  // Замените на ваш домен
    ТокенАвторизации = "мой-секретный-токен-для-1с-2024";  // Тот же токен, что в settings.py
    
    // Получаем товары из справочника
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |    Номенклатура.Ссылка КАК Товар,
    |    Номенклатура.Артикул КАК Артикул,
    |    Номенклатура.Наименование КАК Наименование,
    |    Номенклатура.Описание КАК Описание,
    |    Номенклатура.Цена КАК Цена,
    |    Номенклатура.СтараяЦена КАК СтараяЦена,
    |    Номенклатура.Остаток КАК Остаток,
    |    Номенклатура.Категория.Наименование КАК Категория
    |ИЗ
    |    Справочник.Номенклатура КАК Номенклатура
    |ГДЕ
    |    НЕ Номенклатура.ПометкаУдаления
    |    И Номенклатура.Активен";
    
    РезультатЗапроса = Запрос.Выполнить();
    Выборка = РезультатЗапроса.Выбрать();
    
    МассивТоваров = Новый Массив;
    
    Пока Выборка.Следующий() Цикл
        
        Товар = Новый Структура;
        Товар.Вставить("Артикул", Строка(Выборка.Артикул));
        Товар.Вставить("Наименование", Выборка.Наименование);
        Товар.Вставить("Описание", Строка(Выборка.Описание));
        Товар.Вставить("Цена", Выборка.Цена);
        Товар.Вставить("СтараяЦена", Выборка.СтараяЦена);
        Товар.Вставить("Остаток", Выборка.Остаток);
        Товар.Вставить("Категория", Строка(Выборка.Категория));
        Товар.Вставить("Активен", Истина);
        Товар.Вставить("Характеристики", Новый Массив);  // Можно добавить характеристики
        
        МассивТоваров.Добавить(Товар);
        
    КонецЦикла;
    
    // Отправляем на сайт
    ОтправитьТоварыНаСайт(МассивТоваров, URLСайта, ТокенАвторизации);
    
КонецПроцедуры
```

---

## Часть 3: Проверка работы интеграции

### Шаг 1: Тестовый запрос из 1С

Выполните отправку товаров из 1С (используя код выше).

### Шаг 2: Проверка в админке Django

1. **Откройте админку:** `http://localhost:8000/admin/` (или ваш домен)

2. **Войдите в систему** с правами администратора

3. **Перейдите в раздел "Catalog" → "Sync logs"** (`/admin/catalog/synclog/`)

4. **Проверьте последнюю запись:**
   - **Operation type:** `api_sync` или `file_upload`
   - **Status:** `success` (зеленый) - это означает код 200!
   - **Processed count:** количество обработанных товаров
   - **Created count:** количество созданных товаров
   - **Updated count:** количество обновленных товаров
   - **Errors count:** должно быть 0 для успешной синхронизации

5. **Откройте запись лога** для просмотра деталей:
   - **Request IP:** IP адрес, с которого пришел запрос
   - **Request format:** формат данных (JSON, CSV, XML)
   - **Processing time:** время обработки в секундах
   - **Errors:** список ошибок (если есть)

### Шаг 3: Проверка товаров

1. **Перейдите в "Catalog" → "Products"** (`/admin/catalog/product/`)

2. **Проверьте, что товары созданы/обновлены:**
   - Найдите товары по артикулу, которые вы отправили из 1С
   - Проверьте, что цены, остатки и другие данные обновились

3. **Проверьте характеристики:**
   - Откройте товар
   - В разделе "Product characteristics" должны быть характеристики, если вы их отправили

### Шаг 4: Проверка через API напрямую (опционально)

Можно протестировать API напрямую с помощью curl или Postman:

```bash
curl -X POST http://localhost:8000/api/1c/sync/ \
  -H "Content-Type: application/json" \
  -H "X-API-Key: мой-секретный-токен-для-1с-2024" \
  -d '{
    "token": "мой-секретный-токен-для-1с-2024",
    "products": [
      {
        "sku": "TEST001",
        "name": "Тестовый товар",
        "price": 1000.00,
        "stock": 10,
        "is_active": true
      }
    ]
  }'
```

**Ожидаемый ответ (200 OK):**
```json
{
  "status": "success",
  "processed": 1,
  "created": 1,
  "updated": 0,
  "errors": []
}
```

---

## Часть 4: Что означает статус 200 в логах

### Успешные статусы:

1. **Status: `success`** (код 200)
   - Все товары обработаны успешно
   - Нет ошибок
   - `errors_count = 0`

2. **Status: `partial`** (код 207)
   - Часть товаров обработана успешно
   - Есть ошибки, но некоторые товары созданы/обновлены
   - `errors_count > 0`, но `created_count + updated_count > 0`

### Ошибочные статусы:

1. **Status: `error`** (код 400/500)
   - Критическая ошибка
   - Товары не обработаны
   - `errors_count > 0`, `created_count = 0`, `updated_count = 0`

2. **Status: `unauthorized`** (код 401)
   - Неверный токен авторизации
   - Проверьте токен в `settings.py` и в 1С

---

## Часть 5: Типичные проблемы и решения

### Проблема 1: Статус 401 (Unauthorized)

**Причина:** Неверный токен авторизации

**Решение:**
1. Проверьте токен в `config/settings.py` (переменная `ONE_C_API_KEY`)
2. Проверьте токен в коде 1С (должен совпадать)
3. Убедитесь, что токен передается в заголовке `X-API-Key` или в теле запроса как `token`

### Проблема 2: Статус 400 (Bad Request)

**Причина:** Ошибка валидации данных

**Решение:**
1. Откройте лог синхронизации в админке
2. Посмотрите раздел "Errors" - там будут описания ошибок
3. Проверьте формат данных в 1С:
   - Обязательное поле `sku` (артикул)
   - Поле `price` должно быть числом
   - Поле `stock` должно быть целым числом

### Проблема 3: Товары не создаются

**Причина:** Ошибки в данных или дубликаты артикулов

**Решение:**
1. Проверьте логи синхронизации на наличие ошибок
2. Убедитесь, что артикулы уникальны
3. Проверьте, что все обязательные поля заполнены

### Проблема 4: HTTPS соединение не работает

**Причина:** Проблемы с SSL сертификатом

**Решение:**
1. Для разработки используйте HTTP (порт 80)
2. Для продакшена убедитесь, что SSL сертификат валиден
3. В 1С можно отключить проверку сертификата (только для тестирования):
   ```1c
   Соединение = Новый HTTPСоединение(
       "your-site.com",
       443,
       , , , 30,
       Новый ЗащищенноеСоединениеOpenSSL(, , , , , , , , , , Ложь)  // Отключить проверку сертификата
   );
   ```

---

## Часть 6: Автоматизация в 1С

### Настройка регламентного задания

Для автоматической синхронизации можно создать регламентное задание в 1С:

1. **Создайте обработку** с процедурой синхронизации
2. **Настройте регламентное задание:**
   - Периодичность: ежедневно, еженедельно или по расписанию
   - Процедура: ваша функция отправки товаров
3. **Запустите задание** - синхронизация будет выполняться автоматически

---

## Итоговая проверка

После выполнения всех шагов вы должны увидеть:

1. ✅ В админке Django: `/admin/catalog/synclog/`
   - Новая запись с `Status: success`
   - `Errors count: 0`
   - Данные о количестве обработанных товаров

2. ✅ В админке Django: `/admin/catalog/product/`
   - Товары созданы или обновлены
   - Цены, остатки и характеристики обновлены

3. ✅ В 1С:
   - Сообщение об успешной отправке
   - Код ответа 200 или 207

Если всё это работает - интеграция настроена правильно! 🎉
