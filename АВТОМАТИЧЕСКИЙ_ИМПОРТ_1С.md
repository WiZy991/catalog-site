# Автоматический импорт товаров от 1С

## ✅ Что реализовано

### 1. Автоматическая обработка при получении файла

Когда 1С выгружает XML файл на сервер через CommerceML протокол:
- ✅ Файл автоматически сохраняется
- ✅ Если это XML - автоматически запускается обработка в фоновом режиме
- ✅ Если это ZIP - автоматически распаковывается и обрабатываются все XML файлы внутри

**Пользователю больше не нужно вручную импортировать файлы!**

### 2. Management команда для обработки существующих файлов

Если файлы уже есть на сервере, можно обработать их командой:

```bash
cd ~/onesimus/onesimus
source venv/bin/activate
python manage.py process_1c_files
```

**Опции команды:**
- `--all` - обработать все файлы, даже если они уже обработаны
- `--file filename.xml` - обработать конкретный файл

## 🔧 Настройка автоматической обработки по расписанию

### Вариант 1: Через cron (рекомендуется)

Настройте cron для автоматической проверки новых файлов:

```bash
# Откройте crontab
crontab -e

# Добавьте строку (проверка каждый час)
0 * * * * cd /home/o/onesim8n/onesimus/onesimus && /home/o/onesim8n/onesimus/venv/bin/python manage.py process_1c_files >> /home/o/onesim8n/onesimus/onesimus/logs/cron_1c_import.log 2>&1

# Или проверка раз в день (например, в 3:00 ночи)
0 3 * * * cd /home/o/onesim8n/onesimus/onesimus && /home/o/onesim8n/onesimus/venv/bin/python manage.py process_1c_files >> /home/o/onesim8n/onesimus/onesimus/logs/cron_1c_import.log 2>&1
```

**Важно:** Замените пути на ваши реальные пути!

### Вариант 2: Через панель управления Beget

Если в панели управления Beget есть планировщик задач (Cron), настройте там:

**Команда:**
```bash
cd /home/o/onesim8n/onesimus/onesimus && /home/o/onesim8n/onesimus/venv/bin/python manage.py process_1c_files
```

**Расписание:** Каждый час или раз в день (в зависимости от частоты обмена с 1С)

## 📋 Как это работает

### Процесс обмена с 1С:

1. **1С инициирует обмен** → вызывает `checkauth`
2. **1С запрашивает параметры** → вызывает `init`
3. **1С выгружает файл** → вызывает `file` (файл сохраняется и **автоматически обрабатывается**)
4. **1С завершает обмен** → вызывает `import` (если нужно)

### Если файл уже есть на сервере:

1. Запустите команду `process_1c_files`
2. Команда найдет все XML файлы в `media/1c_exchange/`
3. Обработает необработанные файлы
4. Создаст маркер `.processed` для обработанных файлов

## 🔍 Проверка работы

### Проверить логи обработки:

```bash
tail -f logs/django.log | grep -i "обработк\|товар\|commerceml"
```

### Проверить обработанные файлы:

```bash
ls -la media/1c_exchange/*.processed
```

### Запустить обработку вручную:

```bash
cd ~/onesimus/onesimus
source venv/bin/activate
python manage.py process_1c_files
```

## ⚙️ Настройка в 1С

В 1С настройте автоматический обмен:

1. Откройте настройки обмена с сайтом
2. Установите расписание (например, раз в день в 2:00 ночи)
3. 1С будет автоматически выгружать файлы
4. Сайт автоматически обработает файлы при получении

## 📊 Мониторинг

### Логи автоматической обработки:

Все действия записываются в `logs/django.log`:
- Получение файла от 1С
- Начало обработки
- Прогресс обработки (каждые 100 товаров)
- Результат обработки

### Статистика обработки:

После обработки в логах будет:
```
✓ Файл обработан: обработано 1500 товаров (создано: 200, обновлено: 1300)
```

## ⚠️ Важно

1. **Таймауты:** Если файлы очень большие, может потребоваться увеличение таймаутов (см. инструкцию выше)

2. **Права на файлы:** Убедитесь, что директория `media/1c_exchange/` доступна для записи:
   ```bash
   chmod 755 media/1c_exchange/
   ```

3. **Место на диске:** Большие XML файлы занимают место, можно настроить автоматическое удаление старых файлов

## 🗑️ Очистка старых файлов (опционально)

Можно добавить в cron задачу для очистки старых файлов:

```bash
# Удалить файлы старше 7 дней
find /home/o/onesim8n/onesimus/onesimus/media/1c_exchange/ -type f -mtime +7 -delete
```

## ✅ Итог

Теперь процесс полностью автоматизирован:

1. **1С выгружает файл** → файл автоматически обрабатывается
2. **Или по расписанию** → команда проверяет новые файлы и обрабатывает их
3. **Пользователю не нужно ничего делать вручную!**
