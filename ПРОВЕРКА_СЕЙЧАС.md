# Проверка обмена с 1С - ПРЯМО СЕЙЧАС

## ✅ Хорошая новость
В списке URL паттернов видно, что основной endpoint работает:
- `cml/exchange/` ✅
- `cml/exchange` ✅  
- `1c_exchange.php` ✅

## 🔍 Что проверить СЕЙЧАС:

### 1. Проверьте основной endpoint в браузере:

Откройте в браузере:
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Ожидаемый результат:**
- Должно появиться: `failure\nОшибка авторизации`
- Это **НОРМАЛЬНО** - значит endpoint работает, просто нет авторизации в браузере

**Если не открывается или 404:**
- Проблема с URL или маршрутизацией
- Проверьте настройки на Beget

### 2. Проверьте URL в настройках 1С:

**Правильный URL должен быть:**
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Варианты (попробуйте оба):**
1. `http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth`
2. `http://onesim8n.beget.tech/cml/exchange/?type=catalog&mode=checkauth`

### 3. Проверьте файл логов на сервере:

**Файл:** `logs/commerceml_requests.log`

**Как проверить:**
1. Зайдите на сервер через FTP/SSH/файловый менеджер Beget
2. Откройте файл `logs/commerceml_requests.log`
3. Если файл пустой или нет записей → запросы НЕ доходят до Django

**Если файла нет:**
- Создайте директорию `logs/` если её нет
- Проверьте права доступа

### 4. Выполните обмен в 1С:

1. **В 1С:** Нажмите "Выполнить обмен данными"
2. **Сразу после этого проверьте:**
   - Файл `logs/commerceml_requests.log` - должны появиться новые записи
   - Файл `logs/django.log` - должны появиться записи с "CommerceML запрос получен!"
   - Директорию `media/1c_exchange/` - должны появиться XML файлы

## 🚨 Если запросы НЕ доходят:

### Причина 1: Неправильный URL в 1С

**Проверьте:**
- Нет ли лишних слэшей
- Правильный ли домен (`onesim8n.beget.tech`)
- Есть ли `http://` в начале
- Нет ли пробелов в начале/конце

### Причина 2: Проблемы с доступностью

**Проверьте:**
- Открывается ли `http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth` в браузере
- Если не открывается → проблема с доступностью сайта

### Причина 3: Проблемы с авторизацией

**Если запросы доходят, но не проходят авторизацию:**
- Проверьте логин и пароль в 1С
- Проверьте, что пользователь Django активен и имеет права **staff**
- Проверьте файл `logs/commerceml_requests.log` - там будут записи о запросах

## 📋 Чек-лист диагностики:

- [ ] Endpoint `/cml/exchange?type=catalog&mode=checkauth` открывается в браузере (даже с ошибкой авторизации)
- [ ] URL в 1С правильный
- [ ] В 1С проверка подключения проходит успешно
- [ ] Файл `logs/commerceml_requests.log` создается
- [ ] После обмена в 1С появляются записи в `logs/commerceml_requests.log`
- [ ] После обмена в 1С появляются записи в `logs/django.log`
- [ ] Директория `media/1c_exchange/` существует
- [ ] После обмена в 1С появляются файлы в `media/1c_exchange/`

## 🔧 Что делать дальше:

1. **Проверьте основной endpoint** - откройте в браузере URL выше
2. **Проверьте URL в 1С** - убедитесь, что он правильный
3. **Выполните обмен в 1С** - нажмите "Выполнить обмен данными"
4. **Сразу проверьте логи** - откройте `logs/commerceml_requests.log`
5. **Если записей нет** → запросы не доходят (проблема с URL или сетью)

## 💡 Важно:

- Если endpoint открывается в браузере → Django работает правильно
- Если в 1С "Проверка подключения" проходит → URL правильный
- Если после обмена нет записей в логах → запросы не доходят до Django
- Если есть записи в логах, но файлы не сохраняются → проблема с сохранением файлов

## 📞 Что прислать, если не работает:

1. Скриншот настроек узла обмена в 1С (с URL)
2. Результат открытия `/cml/exchange?type=catalog&mode=checkauth` в браузере
3. Содержимое `logs/commerceml_requests.log` (если файл есть)
4. Последние 50 строк из `logs/django.log`
5. Результат проверки подключения в 1С
