# Обновление изменений в админке

## ⚠️ Проблема
Изменения не отображаются в админке после обновления кода.

## 🔧 Решение

### Шаг 1: Загрузите обновленные файлы на сервер

Загрузите следующие файлы на сервер:

1. **`catalog/forms.py`** - добавлен XML в список форматов
2. **`catalog/admin_views.py`** - добавлена обработка XML
3. **`catalog/services.py`** - добавлена поддержка external_id
4. **`templates/admin/catalog/bulk_product_import.html`** - обновлен шаблон (добавлен XML в список)

### Шаг 2: Очистите кеш браузера

1. Нажмите `Ctrl + F5` (или `Ctrl + Shift + R`) для жесткой перезагрузки страницы
2. Или очистите кеш браузера полностью

### Шаг 3: Перезапустите Django (если нужно)

Если изменения все еще не видны:

1. **Через панель Beget:**
   - Найдите раздел "Управление проектом" или "Django"
   - Нажмите "Перезапустить приложение"

2. **Или подождите** - на некоторых хостингах перезапуск происходит автоматически

### Шаг 4: Проверьте результат

1. Зайдите в админку: `/admin/catalog/bulk-import/`
2. Проверьте:
   - В списке поддерживаемых форматов должен быть **XML**
   - В поле выбора файла должен быть `.xml` в списке форматов
   - В подсказке должно быть: "Загрузите CSV, Excel или XML файл с товарами (поддерживается CommerceML 2 формат)"

## 📋 Что должно появиться:

### В форме импорта:

1. **Список форматов:**
   - CSV
   - Excel (.xls, .xlsx)
   - **XML** ← должно появиться

2. **Поле выбора файла:**
   - Должно принимать файлы: `.csv, .xls, .xlsx, .xml`

3. **Подсказка:**
   - "Загрузите CSV, Excel или XML файл с товарами (поддерживается CommerceML 2 формат)"

## 🔍 Если изменения все еще не видны:

### Проверка 1: Файлы загружены?

1. Проверьте на сервере:
   - `catalog/forms.py` - должна быть строка с `'xml'` в `allowed_extensions`
   - `templates/admin/catalog/bulk_product_import.html` - должна быть строка с XML

### Проверка 2: Кеш браузера

1. Откройте админку в режиме инкогнито
2. Или очистите кеш браузера полностью

### Проверка 3: Django перезапущен?

1. Проверьте логи Django на наличие ошибок
2. Попробуйте перезапустить Django вручную

### Проверка 4: Правильный путь к шаблону?

1. Убедитесь, что шаблон находится по пути:
   - `templates/admin/catalog/bulk_product_import.html`

## 💡 Быстрая проверка:

1. Откройте `/admin/catalog/bulk-import/`
2. Посмотрите на список форматов - должен быть XML
3. Попробуйте выбрать файл - должен быть `.xml` в списке
4. Если не видно - очистите кеш браузера (`Ctrl + F5`)

## 🚨 Если ничего не помогает:

1. Проверьте, что все файлы загружены на сервер
2. Проверьте права доступа к файлам
3. Проверьте логи Django на ошибки
4. Попробуйте открыть админку в другом браузере
