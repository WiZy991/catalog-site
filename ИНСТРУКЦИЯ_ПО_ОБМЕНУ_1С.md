# Инструкция по настройке и диагностике обмена с 1С

## ✅ Что уже настроено:

1. **URL маршруты** - настроены пути для обмена:
   - `/cml/exchange/` - основной путь
   - `/cml/exchange` - без слэша (для совместимости)
   - `/1c_exchange.php` - для совместимости со старыми версиями 1С

2. **Обработка CommerceML 2** - реализована полная поддержка протокола:
   - Режим `checkauth` - проверка авторизации
   - Режим `init` - получение параметров обмена
   - Режим `file` - загрузка файлов
   - Режим `import` - обработка данных

3. **Логирование** - все обмены логируются:
   - В модель `SyncLog` (доступна в админке)
   - В файл `logs/django.log`
   - В консоль (при DEBUG=True)

## 🔧 Настройка в 1С:

### Шаг 1: Создание узла обмена

1. В 1С перейдите: **Администрирование → Обмен данными → Узлы обмена с сайтами**
2. Нажмите **Создать**
3. Заполните поля:
   - **Наименование**: любое (например, "Обмен с сайтом")
   - **Выгрузка товаров**: ✓ (отметьте галочку)
   - **Адрес сайта**: `http://ваш_сайт/cml/exchange?type=catalog&mode=checkauth`
   - **Имя пользователя**: логин пользователя Django с правами staff
   - **Пароль**: пароль этого пользователя

### Шаг 2: Проверка подключения

1. Нажмите кнопку **"Проверить соединение..."**
2. Должно появиться сообщение: **"Соединение успешно установлено"**
3. Если ошибка - проверьте:
   - Правильность URL
   - Доступность сайта из сети, где находится 1С
   - Правильность логина и пароля

### Шаг 3: Настройка выгрузки товаров

1. Перейдите на вкладку **"Выгрузка товаров"**
2. Укажите:
   - **Организация** - владелец каталога
   - **Выгружать файлы изображений**: ✓ (если нужны картинки)
   - **Каталоги** - настройте какие товары выгружать

### Шаг 4: Выполнение обмена

1. Нажмите кнопку **"Выполнить обмен данными"**
2. Дождитесь завершения
3. Должно появиться сообщение: **"Обмен завершен успешно"**

## 🔍 Диагностика проблем:

### Проблема: "Обмен завершен успешно", но товары не появляются

**Что проверить:**

1. **Логи синхронизации в админке Django:**
   - Зайдите: `/admin/catalog/synclog/`
   - Посмотрите последнюю запись:
     - **Статус** - должен быть "Успешно" или "Частично"
     - **Обработано товаров** - должно быть > 0
     - **Создано товаров** - должно быть > 0 (если товары новые)
     - **Ошибок** - проверьте, есть ли ошибки

2. **Файлы обмена:**
   - Проверьте директорию: `media/1c_exchange/`
   - Должны быть XML файлы (например, `import0_1.xml`)
   - Если файлов нет → файлы не сохраняются

3. **Логи Django:**
   - Файл: `logs/django.log`
   - Ищите строки: "CommerceML запрос получен!"
   - Если их нет → запросы не доходят до Django

4. **Товары в админке:**
   - Зайдите: `/admin/catalog/product/`
   - Отфильтруйте по `external_id` (не пустое)
   - Есть ли товары с `external_id`?

### Типичные ошибки:

#### ❌ Ошибка: "Файлы не сохраняются"
**Причина:** Проблемы с правами доступа или директорией
**Решение:**
- Проверьте, что директория `media/1c_exchange/` существует
- Проверьте права доступа к директории
- Проверьте логи Django на ошибки сохранения

#### ❌ Ошибка: "Товары не создаются"
**Причина:** Ошибки валидации или неправильный формат XML
**Решение:**
- Проверьте ошибки в логах синхронизации
- Убедитесь, что в XML есть обязательные поля: `Ид` и `Наименование`
- Проверьте формат данных (цена - число, количество - целое число)

#### ❌ Ошибка: "Запросы не доходят до Django"
**Причина:** Неправильный URL или проблемы с сетью
**Решение:**
- Проверьте URL в настройках 1С
- Проверьте, что сайт доступен из сети, где находится 1С
- Проверьте логи веб-сервера (nginx/apache)

## 📋 Чек-лист проверки:

- [ ] URL в 1С указан правильно: `http://ваш_сайт/cml/exchange?type=catalog&mode=checkauth`
- [ ] Проверка подключения в 1С проходит успешно
- [ ] Логин и пароль соответствуют пользователю Django с правами staff
- [ ] В админке Django есть записи в "Логи синхронизации"
- [ ] В директории `media/1c_exchange/` есть XML файлы
- [ ] В логах синхронизации статус "Успешно" или "Частично"
- [ ] Количество обработанных товаров > 0
- [ ] В админке есть товары с `external_id`

## 🚀 Быстрая диагностика:

1. Запустите обмен в 1С
2. Сразу проверьте `/admin/catalog/synclog/` - должна появиться новая запись
3. Если записи нет → обмен не доходит до Django (проверьте URL и сеть)
4. Если запись есть, но товаров нет → проверьте ошибки в логе
5. Если товары создаются, но не видны на сайте → проверьте `is_active` и категорию

## 📚 Дополнительные материалы:

- `DIAGNOSE_1C_EXCHANGE.md` - подробная диагностика
- `QUICK_CHECK_1C.md` - быстрая проверка
- `1C.txt` - документация по протоколу CommerceML 2

## 💡 Полезные команды:

### Проверка логов через Python:
```python
from catalog.models import SyncLog
# Последние 5 логов
logs = SyncLog.objects.all()[:5]
for log in logs:
    print(f"{log.created_at}: {log.status} - обработано: {log.processed_count}, создано: {log.created_count}")
```

### Проверка товаров с external_id:
```python
from catalog.models import Product
# Товары из 1С
products = Product.objects.exclude(external_id__isnull=True).exclude(external_id='')
print(f"Товаров из 1С: {products.count()}")
```

## ⚠️ Важные замечания:

1. **URL должен заканчиваться правильно:**
   - ✅ `.../cml/exchange?type=catalog&mode=checkauth`
   - ✅ `.../cml/exchange/?type=catalog&mode=checkauth`
   - ❌ `.../cml/exchange/checkauth` (неправильно!)

2. **Авторизация:**
   - Используется базовая HTTP авторизация (Basic Auth)
   - Логин и пароль должны быть от пользователя Django с правами **staff**
   - Пользователь должен быть **активным**

3. **Формат данных:**
   - 1С должен выгружать данные в формате **CommerceML 2**
   - Файлы должны содержать элементы `<Каталог>` или `<ПакетПредложений>`
   - Обязательные поля: `Ид` (идентификатор) и `Наименование` (название)

4. **Обработка файлов:**
   - Файлы сохраняются в `media/1c_exchange/`
   - После сохранения файлы обрабатываются и удаляются (опционально)
   - Обработка происходит в транзакции (все или ничего)

## 🆘 Если ничего не помогает:

1. Проверьте логи Django: `logs/django.log`
2. Проверьте логи веб-сервера (nginx/apache)
3. Проверьте, что Django запущен и доступен
4. Попробуйте выполнить обмен еще раз
5. Проверьте настройки в 1С еще раз

Если проблема сохраняется, пришлите:
- Скриншот логов синхронизации из админки
- Последние 50 строк из `logs/django.log`
- Пример XML файла из `media/1c_exchange/` (первые 100 строк)
