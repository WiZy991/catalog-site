# Диагностика проблемы с файлом логов

## ✅ Хорошая новость
Код обновлен на сервере - логирование в файл есть!

## ⚠️ Проблема
Файл `logs/commerceml_requests.log` не создается, хотя код есть.

## 🔍 Возможные причины:

### 1. Запросы не доходят до Django

**Проверка:**
Откройте в браузере:
```
http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth
```

**Ожидаемый результат:**
- Должно появиться: `failure\nОшибка авторизации`
- После этого проверьте, создался ли файл `logs/commerceml_requests.log`

**Если файл создался:**
- ✅ Код работает правильно
- ❌ Запросы от 1С не доходят до Django

**Если файл НЕ создался:**
- Проблема с правами доступа или путями (см. ниже)

### 2. Проблема с правами доступа

**Проверка:**
1. Убедитесь, что директория `logs/` существует
2. Проверьте права доступа:
   - Директория `logs/` должна иметь права 755
   - Django должен иметь права на запись в эту директорию

**Решение:**
1. Создайте директорию `logs/` если её нет
2. Установите права 755 на директорию
3. Проверьте, что веб-сервер (nginx/apache) имеет права на запись

### 3. Проблема с путем к файлу

**Проверка:**
Код использует `settings.BASE_DIR` для определения пути. Проверьте:
- Что `BASE_DIR` указывает на правильную директорию
- Что путь `logs/commerceml_requests.log` правильный

**Решение:**
После открытия endpoint в браузере проверьте логи Django (`logs/django.log`) на наличие ошибок:
- `Ошибка создания директории logs/`
- `Ошибка записи в файл логов`

### 4. Проблема с кодировкой или форматом

**Проверка:**
Код использует `encoding='utf-8'` и `f.flush()` для принудительной записи.

## 🔧 Что делать:

### Шаг 1: Проверьте endpoint в браузере

1. Откройте: `http://onesim8n.beget.tech/cml/exchange?type=catalog&mode=checkauth`
2. Должно появиться: `failure\nОшибка авторизации`
3. **Сразу после этого** проверьте:
   - Создался ли файл `logs/commerceml_requests.log`
   - Есть ли ошибки в `logs/django.log`

### Шаг 2: Проверьте директорию logs/

1. Убедитесь, что директория `logs/` существует
2. Если нет → создайте её
3. Установите права 755

### Шаг 3: Проверьте логи Django

Откройте `logs/django.log` и ищите:
- `Ошибка создания директории logs/`
- `Ошибка записи в файл логов`
- `CommerceML запрос получен!` (если запросы доходят)

### Шаг 4: Выполните обмен в 1С

1. В 1С нажмите "Выполнить обмен данными"
2. **Сразу после этого** проверьте:
   - Файл `logs/commerceml_requests.log` - должен появиться или обновиться
   - Файл `logs/django.log` - должны появиться записи

## 📋 Чек-лист:

- [ ] Endpoint открывается в браузере
- [ ] После открытия endpoint создается файл `logs/commerceml_requests.log`
- [ ] Директория `logs/` существует
- [ ] Права доступа к `logs/` правильные (755)
- [ ] В `logs/django.log` нет ошибок записи в файл
- [ ] После обмена в 1С появляются записи в `logs/commerceml_requests.log`

## 💡 Важно:

- Если файл создается после открытия endpoint в браузере → код работает, просто запросы от 1С не доходят
- Если файл НЕ создается даже после открытия endpoint → проблема с правами доступа или путями
- Проверьте логи Django на наличие ошибок записи в файл

## 🚨 Если файл все еще не создается:

1. Проверьте права доступа к директории проекта
2. Проверьте, что веб-сервер имеет права на запись
3. Проверьте логи Django на ошибки
4. Попробуйте создать файл вручную и проверить права
5. Проверьте, что `BASE_DIR` указывает на правильную директорию
